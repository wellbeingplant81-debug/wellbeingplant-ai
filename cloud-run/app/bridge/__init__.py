"""AI Bridge 가 이 제품을 부를 수 있게 하는 **얇은 경계 하나**.

여기에 있는 것은 감싸개뿐이고 제품 기능은 하나도 없다. 이 패키지가 커지면
그것은 경계가 아니라 두 번째 파이프라인이라는 뜻이다.

**`ai_bridge` 도, `app.pipeline` 도 import 하지 않는다.** 앞의 것은 경계이고,
뒤의 것은 무게다 -- import 하는 순간 `requirements.txt` 77줄이 전부 필요해진다.
부를 것은 주입으로 받는다.
"""
